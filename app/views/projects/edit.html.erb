<%= turbo_frame_tag dom_id(@project) do %>
  <div class="card card-body">
    <h5 class="font-bold text-4xl">Editing project</h5>
    <%= render "form", project: @project %>
    <%= link_to "Cancel", projects_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
  </div>
<% end %>
